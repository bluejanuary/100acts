{
  "name": "100acts",
  "private": true,
  "workspaces": [
    "mobile",
    "app"
  ],
  "scripts": {
    "mobile": "yarn workspace mobile start",
    "app": "yarn workspace app100acts dev",
    "build": "yarn workspace app100acts build",
    "start": "yarn workspace app100acts start"
  },
  "engines": {
    "node": "24.x",
    "yarn": ">=1.x"
  }
}
